id = "project_scan"
name = "Scan Project"
version = "0.1.0"
description = "Scans a project tree while ignoring common generated and dependency directories."
risk_level = "low"
permissions = ["project_scan", "file_system_read"]
platforms = ["windows", "linux", "macos"]
entrypoint = "builtin:project_scan"
author = "Axiom Agent"
license = "MIT"

[updates]
auto_update = false
channel = "stable"

[input_schema]
type = "object"

[output_schema]
type = "object"

