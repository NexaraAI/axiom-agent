id = "file_write_text"
name = "Write Text File"
version = "0.1.0"
description = "Writes UTF-8 text to a file inside the active workspace after confirmation."
risk_level = "medium"
permissions = ["file_system_write"]
platforms = ["windows", "linux", "macos"]
entrypoint = "builtin:file_write_text"
author = "Axiom Agent"
license = "MIT"

[updates]
auto_update = false
channel = "stable"

[input_schema]
type = "object"

[output_schema]
type = "object"

