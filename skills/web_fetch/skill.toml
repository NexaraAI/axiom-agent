id = "web_fetch"
name = "Fetch Web Text"
version = "0.1.0"
description = "Fetches URL text with timeout and response size limits."
risk_level = "medium"
permissions = ["network"]
platforms = ["windows", "linux", "macos"]
entrypoint = "builtin:web_fetch"
author = "Axiom Agent"
license = "MIT"

[updates]
auto_update = false
channel = "stable"

[input_schema]
type = "object"

[output_schema]
type = "object"

