id = "file_read_text"
name = "Read Text File"
version = "0.1.0"
description = "Reads UTF-8 text from a file inside the active workspace."
risk_level = "low"
permissions = ["file_system_read"]
platforms = ["windows", "linux", "macos"]
entrypoint = "builtin:file_read_text"
author = "Axiom Agent"
license = "MIT"

[updates]
auto_update = false
channel = "stable"

[input_schema]
type = "object"

[output_schema]
type = "object"

